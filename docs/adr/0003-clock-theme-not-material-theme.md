# 产品面孔是 ClockTheme，不用 AdaptiveTheme

V1 去掉 AdaptiveTheme。Clock 只认 ClockTheme + Night Mode。Settings 用固定深色 chrome。

ClockTheme（Minimal/Flip/OLED/Retro）不是 Material 亮暗色。两套「主题」并存会让 Night Mode、OLED、系统暗色无法分辨。
