# Night Mode 是显示策略，不是 ClockTheme

Night Mode 只降亮、减信息，可叠在任意 ClockTheme 上。OLED 仍是一个 ClockTheme，不是 Night Mode。V1 是否提供 Auto 见后续 ADR。

PRD 把 Night Mode、OLED、Night Clock 混在一起；若把 Night Mode 做成第五套 Theme，床头场景就绑死一种面孔。
